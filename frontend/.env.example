# Sylvy, the planning analyst in the right rail.
#
# With a key set, Sylvy reasons over the live plan through Kimi K3 on NVIDIA's
# API. Without one, the /api/sylvy route falls back to a deterministic
# responder built from the same briefing, so the interface still answers —
# just more narrowly.
NVIDIA_API_KEY=

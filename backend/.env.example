APP_ENV=development
# JSON array; exact browser origins, without trailing slashes.
CORS_ORIGINS=["http://localhost:3000"]
SUPABASE_URL=
# Same publishable key as the frontend. No service-role key needed yet.
SUPABASE_PUBLISHABLE_KEY=
